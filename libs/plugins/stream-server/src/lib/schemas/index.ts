const settings = require('./action-settings.json');
const resource = require('./resource.json');
const data = require('./stream-data.json');
const common = require('./common.json');
const internalData = require('./internal-schema.json');

export const StreamSchemas = { settings, resource, data, common, internalData };
